<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Hello <%= User.name %> </h1>
    <h1>Current win/lost: 
        <% if(User.loss === 0){%>
            <%=User.win %> Wins and <%=User.loss%> Losses
        <%}else{%>
            <%=User.win / User.lost %>
        <%}%>
    </h1>
    <a href="/game"><button>Play game</button></a>
</body>
</html>